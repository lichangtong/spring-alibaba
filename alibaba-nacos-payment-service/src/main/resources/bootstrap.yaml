#server:
#  port: 8084

spring:
  profiles:
    active: dev
  application:
    name: payment-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        prefix: ${spring.application.name}
        server-addr: 127.0.0.1:8848
        refresh:
          enabled: true
        file-extension: yaml